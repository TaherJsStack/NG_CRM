import { RemoveCardDirective } from './remove-card.directive';

describe('RemoveCardDirective', () => {
  it('should create an instance', () => {
    // const directive = new RemoveCardDirective();
    // expect(directive).toBeTruthy();
  });
});
