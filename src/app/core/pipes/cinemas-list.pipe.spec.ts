import { CinemasListPipe } from './cinemas-list.pipe';

describe('CinemasListPipe', () => {
  it('create an instance', () => {
    const pipe = new CinemasListPipe();
    expect(pipe).toBeTruthy();
  });
});
